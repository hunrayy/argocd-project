kind: Service
apiVersion: v1
metadata:
  name:  argocd-project-service
  namespace: app
spec:
  selector:
    app:  nginx
  type:  LoadBalancer
  ports:
  - name:  name-of-the-port
    port:  80
    targetPort:  80